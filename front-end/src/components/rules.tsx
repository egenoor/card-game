import { Typography } from "@mui/material";

function Rules() {
  return (
    <Typography>
      Each round the player must guess if the next card drawn from the deck is
      equal, higher or lower to the base card.
    </Typography>
  );
}

export default Rules;
